# عقد النشر — Smart Auditor مدقق ذكي

**النطاق:** هذا العقد يحكم منصة `https://89yousef.github.io/gcsd-smart-auditor/` وكل محتواها بلا استثناء. **أي وكيل ذكاء اصطناعي** (حالي أو مستقبلي) يعمل على هذا المستودع **ملزَم** باتّباعه حرفياً قبل أي نشر (`git push`).

**آخر اعتماد:** 07/09/2026 — بعد جلسة تدقيق حي كشفت 3 عيوب حقيقية (Dark Mode CSS مكسور، تخطيط جوال متكسّر، تشوّش RTL/LTR) لم تكن مرئية بدون اختبار فعلي.

---

## 1) نطاق المواضيع المعتمدة (Topic Governance)

- **الموضوع الوحيد المعتمَد حالياً: "أصناف البيع بربح وخسارة" (Items Sold at Profit & Loss).**
- **ممنوع** على أي وكيل إضافة قسم/موضوع/تقرير جديد للمنصة **إلا بعد اتفاق صريح موثَّق** من GM في المحادثة. وجود فكرة "قيد النقاش" لا يعني الإذن بالنشر.
- عند إضافة موضوع جديد معتمَد لاحقاً: يُضاف كـ`topic-card` جديد داخل القسم المناسب (أو قسم جديد إن لزم)، **مع تطبيق كامل هذا العقد عليه من الصفر** (نفس مستوى التحقق، لا استثناء لأنه "بسيط").

---

## 2) عند استلام أمر "حدّث المنصة" — ماذا يعني بالضبط

أي وكيل يستلم أمر "حدّث المنصة" من GM يُنفّذ **الثلاثة معاً**، بلا استثناء وبلا تجزئة:

1. **تطبيق كل معايير هذا العقد** (الأقسام 3-7 أدناه) على أي كود موجود يُعدَّل.
2. **تحديث بيانات كل تقرير للموضوع/المواضيع المعتمَدة حالياً** — يعني إعادة جلب حية من Zoho لكل النوافذ الزمنية المعرَّفة (حالياً: آخر 30 يوماً / آخر 7 أيام / آخر يوم كامل) **دفعة واحدة في نفس الجلسة**، لا نافذة واحدة فقط.
3. **تطبيق أي موضوع جديد تمّت الموافقة الصريحة عليه** فقط — لا شيء أبعد من ذلك.

**ممنوع** الاكتفاء بتحديث جزئي (مثلاً نافذة 30 يوم فقط) ووصفه بأنه "تحديث المنصة" — هذا يُعتبر إخلالاً بالعقد.

---

## 3) سلامة البيانات (Data Integrity) — إلزامي

- كل رقم مُعروض (KPI، صف جدول، شريط مخطط) يجب أن يُشتَقّ من **جلب حي من Zoho** في نفس جلسة التحديث — لا نسخ من الذاكرة أو تخمين.
- بعد كتابة أي مصفوفة بيانات (`DATA30`, `DATA7`, `DATA1`, ...): تشغيل تحقّق حسابي آلي (مجموع الخسارة، عدد الأصناف، عدد الوحدات) ومطابقته يدوياً مع الأرقام المعروضة في `KPI` — **قبل** الـcommit.
- أي رقم "تقديري" (غير مؤكَّد 100% من النظام) يُصنَّف صراحة في الواجهة بعبارة مثل "تقديري — يحتاج تأكيداً إضافياً"، تمييزاً عن الأرقام المؤكَّدة مباشرة من النظام.
- ممنوع نسب خسارة لموظف/بائع معيّن دون التحقّق أولاً: هل الحالة مركَّزة (فاتورة/عميل واحد) أم موزَّعة (مشكلة تسعير عامة)؟ — القاعدة المعتمَدة من موضوع البيع بخسارة تنطبق على أي موضوع مشابه لاحقاً.

---

## 4) الاختبار البصري (Visual QA) — إلزامي قبل كل push

- **الوضع الفاتح والداكن معاً** — عيب حقيقي (نص غير مقروء) اكتُشِف فقط بتفعيل الوضع الداكن فعلياً، لا بقراءة الكود.
- **الجوال (375px) والديسكتوب معاً.**
- فحص أي نص يخلط عربي/إنجليزي مع أرقام أو أسهم (→) بصرياً — احتمال تشوّش اتجاه (bidi) حقيقي، ليس نظرياً.

## 5) جودة الكود (Code Quality) — إلزامي قبل كل commit

- **ممنوع** كتابة قاعدة CSS بصيغة `selector,@media{...}` — هذه صياغة غير صحيحة تُسقِط القاعدة بصمت بلا أي خطأ ظاهر. أي دمج بين انتقائي (selector) وقاعدة `@media` يجب أن يكون **سطرين منفصلين**.
- أي نص يخلط اتجاهين (عربي RTL + تاريخ/رقم LTR) داخل جملة واحدة يُلَفّ بوسم `<bdi>`.
- تحقّق آلي إلزامي قبل كل commit:
  ```bash
  # توازن وسوم HTML
  python3 -c "
  from html.parser import HTMLParser
  content = open('index.html', encoding='utf-8').read()
  class P(HTMLParser):
      def __init__(self):
          super().__init__(); self.stack=[]; self.void={'meta','link','br','img','input','hr'}
      def handle_starttag(self, tag, attrs):
          if tag not in self.void: self.stack.append(tag)
      def handle_endtag(self, tag):
          if self.stack and self.stack[-1]==tag: self.stack.pop()
          else: print('mismatch', tag, self.stack[-5:])
  p=P(); p.feed(content); print('unclosed:', p.stack)
  "
  # سلامة صياغة JavaScript
  node -e "
  const fs = require('fs');
  const html = fs.readFileSync('index.html','utf8');
  const m = html.match(/<script>([\s\S]*)<\/script>/);
  new Function(m[1]);
  console.log('JS syntax OK');
  "
  # لا صياغة CSS مكسورة
  grep -c ',@media' index.html   # يجب أن تكون النتيجة 0
  ```

## 6) الاختبار الوظيفي (Functional QA) — إلزامي، محاكاة فعلية لا نظر بصري فقط

عبر `javascript_exec` على الصفحة المحلية أو الحية، لكل تحديث:
- محاكاة نقر فرز عمود (`th.click()`) والتحقّق أن الترتيب تغيّر فعلياً.
- محاكاة كتابة في مربع البحث (`input` event) والتحقّق أن عدد الصفوف تغيّر بمنطقية.
- محاكاة نقر فلتر (High/Mid/All) والتحقّق من عدد الصفوف الناتج يطابق حساباً يدوياً.
- التحقّق أن كل زر/بطاقة تنقّل (`goHome`, `goDept`, `goReport`) ينقل فعلاً للعرض الصحيح ويحدّث الـ`breadcrumbs`.
- التحقّق أن لا روابط ميتة: `grep 'href="#"' index.html` يجب أن تُرجع فارغاً.

## 7) الجداول — لا حدّ ارتفاع مخفٍ

- ممنوع استخدام `max-height` + `overflow-y:auto` على حاوية جدول بيانات رئيسي — هذا يُخفي جزءاً من البيانات خلف تمرير داخلي غير واضح للمستخدم ويبدو وكأن البيانات ناقصة. الجدول الكامل يُعرَض ضمن تدفّق الصفحة الطبيعي.

---

## 8) خطوات النشر (Deployment Process) — بالترتيب

1. تطبيق كل التعديلات على `index.html` (أو ملفات الموقع).
2. تشغيل فحوصات القسم 5 (HTML/JS/CSS) — يجب أن تمر كلها بلا أخطاء.
3. تشغيل فحوصات القسم 6 (وظيفي) محلياً.
4. `git add -A && git commit -m "<وصف دقيق للتغيير>"`.
5. **الوكيل لا يملك صلاحية `git push` مباشرة في هذه الجلسة** (قيد أمان من النظام) — يُطلَب من GM تشغيل:
   ```bash
   cd /private/tmp/claude-501/-Users-yousef89/b13cd850-d217-4d8b-aa84-bd1bc961dc80/scratchpad/smart-auditor-site && git push
   ```
6. بعد تأكيد GM أن الأمر نُفِّذ: **التحقّق من الموقع الحي فعلياً** (القسم 9) — لا افتراض أن النشر نجح لمجرد تنفيذ الأمر.

---

## 9) التحقّق بعد النشر (Post-Deployment Verification) — إلزامي، لا يُتخطّى أبداً

بعد كل push مؤكَّد من GM، الوكيل يُنفّذ **كل ما يلي** قبل إخبار GM أن النشر تم بنجاح:

1. **التحقّق أن المحتوى الجديد فعلاً حيّ** (لا نسخة قديمة مخبَّأة):
   ```bash
   until curl -s https://89yousef.github.io/gcsd-smart-auditor/ | grep -q "<نص أو رقم فريد من التحديث الجديد>"; do sleep 3; done; echo "FOUND"
   ```
2. **التحقّق أن الخلل/التحديث المطلوب فعلاً انحلّ** — فتح الصفحة الحية عبر متصفح حقيقي (لا افتراض)، وتنفيذ نفس فحوصات القسم 6 الوظيفية على النسخة الحية مباشرة (`javascript_exec` على تبويب فعلي مفتوح على الرابط الحي).
3. **إعادة اختبار كل تقرير للموضوع/المواضيع المعتمَدة** (لا التغيير الجديد فقط) — للتأكد أن الإصلاح لم يكسر شيئاً كان يعمل سابقاً (فحص رجعي / regression check). في الوضع الحالي: تقرير "أصناف البيع بربح وخسارة" بنوافذه الثلاث (30/7/1 يوم) يُعاد التحقّق منه بالكامل: الأرقام، الجداول، الفرز، البحث، الفلاتر.
4. **تقرير نتيجة صادق لـGM** يتضمّن: ما الذي أُصلِح/أُضيف، ما الذي تحقَّق منه فعلياً (وكيف)، وأي ملاحظة أو قيد لم يُحلّ بعد.

---

## 10) سجل التغييرات (لتحديث يدوي بعد كل نشر)

| التاريخ | الوصف | Commit |
|---|---|---|
| 2026-09-07 | إطلاق أولي: صفحة رئيسية ثنائية اللغة + قسم المشتريات + تقرير البيع بخسارة (3 نوافذ زمنية) | cf68fe9 |
| 2026-09-07 | تحديث الشعار النصي إلى GCSD | 86714f3 |
| 2026-09-07 | بيانات حقيقية لنافذة آخر 7 أيام | 74388c4 |
| 2026-09-07 | إزالة قيد التمرير عن الجداول + بيانات حقيقية لآخر يوم كامل (بدل اليوم الجاري الفارغ) | a0ed308 |
| 2026-09-07 | إصلاح خلل CSS كسر الوضع الليلي (نص غير مقروء) | 09d70f2 |
| 2026-09-07 | إصلاح تخطيط الشريط العلوي على الجوال + تشوّش اتجاه RTL/LTR في نطاقات التاريخ | c6f52ae |
| 2026-09-07 | إضافة هذا العقد (CONTRACT.md) | (هذا الالتزام) |
