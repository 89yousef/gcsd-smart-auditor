# DEPLOY_LOG.md — محاضر النشر

> **قاعدة صارمة: هذا الملف Append-Only.** يُضاف سطر/قسم جديد فقط في الأعلى بعد كل نشر ناجح — **ممنوع تعديل أو حذف أي محضر سابق**، حتى لو تبيّن لاحقاً أنه كان يحتوي خطأً. إن احتجت تصحيح محضر قديم، أضف محضراً جديداً يشير إليه ويوضّح التصحيح — لا تكتب فوقه أبداً. هذا يمنع اللبس بين المحادثات ويحمي من "خطأ يحصل من دون ما أعلم" (طلب GM صراحة).

**قالب كل محضر جديد (انسخه واملأه):**

```
## [التاريخ] — [ملخص من سطر واحد]
**الوكيل:** (اسم/معرّف الجلسة إن وُجد)
**السبب:** لماذا حصل هذا النشر (طلب GM، إصلاح خلل، تحديث بيانات، موضوع جديد معتمَد)
**ما تغيّر:**
- ...
**الفحوصات التي طُبِّقت (CONTRACT.md):**
- [ ] سلامة البيانات (تحقّق حسابي)
- [ ] بصري: فاتح + داكن
- [ ] بصري: جوال + ديسكتوب
- [ ] كود: HTML/JS/CSS سليم
- [ ] وظيفي: فرز/بحث/فلتر/تنقّل
- [ ] لا max-height على الجداول
**Commits:** (hash قصير + رسالة)
**تحقّق ما بعد النشر:** (رابط حي فُحص، ماذا وجدت بالضبط)
**ملاحظات/قيود مفتوحة:** (أي شيء لم يُحلّ، يحتاج قرار GM، إلخ)
```

---

## 2026-09-07 — معالجة تقرير GM: خلل عرض مشتبَه به + حماية دفاعية + توثيق قيد اختبار iframe
**الوكيل:** Claude (نفس جلسة الإطلاق، تكملة)
**السبب:** GM أبلغ أن "آخر يوم" يظهر بصرياً 2 صنف بالـKPI لكن الجدول تحته فارغ — طلب فهم كيف نحكم هذي الأخطاء مستقبلاً.
**ما تغيّر:**
- اختبار حي بنقرة فعلية محاكاة (لا استعلام كود) على الرابط المباشر: الجدول ظهر **صحيحاً وكاملاً** (2 صف مطابقة للـKPI). لم يتكرر الخلل عبر الرابط المباشر.
- اكتُشِف قيد أدوات حقيقي: لا يمكن فحص محتوى iframe خارجي (github.io) داخل صفحة Zoho فعلياً بسبب Same-Origin Policy — يعني كل اختبار "بعد النشر" السابق كان عبر الرابط المباشر فقط، لا داخل Zoho iframe نفسه. وُثِّق هذا القيد صراحة في `CONTRACT.md § 9.5`.
- أُضيفت حماية دفاعية: أي خطأ JavaScript غير متوقَّع الآن يُظهر شريط تحذير أحمر واضح بدل جدول فارغ صامت (`window.onerror` + `try/catch` حول التهيئة) + `<noscript>` تحذير لو JS معطَّل كلياً.
- أُضيف زر "↗ نافذة كاملة" ثابت بأعلى كل صفحة يفتح الرابط المباشر في تبويب جديد — حل بديل فوري لو صار أي تصرّف غريب داخل إطار Zoho.
**الفحوصات التي طُبِّقت (CONTRACT.md):**
- [x] سلامة البيانات (تحقّق حسابي) — غير مطلوب، لا تغيير بيانات
- [x] بصري: فاتح + داكن
- [ ] بصري: جوال + ديسكتوب — لم يُعَد اختباره لهذا التغيير تحديداً (تغيير طفيف بالطوبار)
- [x] كود: HTML/JS/CSS سليم
- [x] وظيفي: نقرة حقيقية على تبويب "آخر يوم" + تحقّق DOM كامل (KPI، أشرطة، جدول)
- [x] لا max-height على الجداول
**Commits:** 81040a6 (main, github.com/89yousef/gcsd-smart-auditor)
**تحقّق ما بعد النشر:** (يُستكمَل بعد تأكيد GM لأمر git push)
**ملاحظات/قيود مفتوحة:**
- **لم يُحسَم بعد** هل الخلل الأصلي (جدول فارغ) كان حقيقياً داخل Zoho iframe تحديداً أو سوء فهم/كاش متصفح — لا أداة عندي تثبت ذلك قطعياً. الحماية الدفاعية + زر النافذة الكاملة يخففان الأثر بغض النظر عن السبب الجذري.
- إن تكرر الخلل لـGM داخل Zoho تحديداً بعد هذا النشر: يُطلَب منه فتح Developer Console (F12) داخل تبويب Zoho ونسخ أي رسالة حمراء — هذا الدليل الوحيد الذي يفتح تشخيصاً حقيقياً لمشكلة iframe.

---

## 2026-09-07 — إطلاق المنصة + جولة تدقيق شاملة + إنشاء العقد والحوكمة
**الوكيل:** Claude (جلسة بناء أولى)
**السبب:** طلب GM بناء منصة "Smart Auditor" داخل Zoho Web Tabs تعرض تقرير البيع بخسارة لكل فريق العمل، ثم طلب تدقيق شامل + عقد نشر رسمي.
**ما تغيّر:**
- إطلاق أولي: صفحة رئيسية ثنائية اللغة (عربي/إنجليزي) + شعار نصي GCSD + هدف نمو 20% + بطاقة "موظف الشهر قريباً" + تنقّل هرمي (رئيسية → قسم المشتريات والمخزون والجرد → تقرير البيع بخسارة).
- تقرير "أصناف البيع بربح وخسارة" بثلاث نوافذ زمنية (30 يوم / 7 أيام / آخر يوم كامل) — جداول قابلة للفرز والبحث والفلترة، بيانات حقيقية 100% من Zoho لكل نافذة.
- بطاقة "مين المسؤول؟": حالة مركَّزة (52.5%، فاتورة SB-INV-WH05887) + مشكلة تسعير عامة موزَّعة (~47.5%).
- تدقيق شامل حي على الموقع المنشور فعلاً كشف 3 عيوب حقيقية وأُصلحت:
  1. خطأ صياغة CSS (`selector,@media`) كسر قراءة النص في الوضع الليلي على 3 عناصر.
  2. تخطيط الشريط العلوي يتكسّر على شاشات الجوال الضيقة.
  3. تشوّش اتجاه بصري (RTL/LTR) في أسطر النطاق الزمني المختلطة عربي/إنجليزي.
- إزالة قيد `max-height` كان يخفي جزءاً من صفوف الجداول خلف تمرير داخلي غير واضح.
- تصحيح مفهوم "آخر يوم": كان يعني "اليوم الجاري" (يظهر فارغاً كل صباح) → صار "آخر يوم مكتمل فعلياً" (أمس) ببيانات حقيقية دائماً.
- إنشاء `CONTRACT.md` (معايير النشر والاختبار الإلزامية لكل وكيل مستقبلي).
- إنشاء `STATE.md` (حالة المشروع الحيّة — نقطة البداية الإلزامية لأي وكيل جديد).
- إنشاء هذا الملف `DEPLOY_LOG.md`.
**الفحوصات التي طُبِّقت (CONTRACT.md):**
- [x] سلامة البيانات (تحقّق حسابي عبر Node.js لكل نافذة زمنية — مطابقة تامة مع DOM المعروض)
- [x] بصري: فاتح + داكن (كشف العيوب أعلاه)
- [x] بصري: جوال + ديسكتوب (كشف عيب التخطيط أعلاه)
- [x] كود: HTML/JS/CSS سليم (فحص آلي بعد كل تعديل)
- [x] وظيفي: فرز/بحث/فلتر/تنقّل (اختبار محاكاة فعلي عبر `javascript_exec` على الموقع الحي، لا افتراض)
- [x] لا max-height على الجداول (أُزيل)
**Commits:** cf68fe9, 86714f3, 74388c4, a0ed308, 09d70f2, c6f52ae, 4d41a76 (main branch, https://github.com/89yousef/gcsd-smart-auditor)
**تحقّق ما بعد النشر:** فُحص كل commit حياً عبر `curl`/`fetch` على `https://89yousef.github.io/gcsd-smart-auditor/` بعد كل `git push` من GM، وعبر متصفح حقيقي (تنقّل + محاكاة نقر/بحث/فرز) للتدقيق الشامل النهائي.
**ملاحظات/قيود مفتوحة:**
- الشعار الحالي (شارة نصية "GCSD") مؤقت — لا يوجد ملف شعار GCSD رسمي بعد. عند توفّره، يُستبدَل بتعديل بسيط في `.brand-mark`/`.hero-mark`.
- الوكيل لا يملك صلاحية `git push` مباشرة في هذه الجلسة — يحتاج GM يشغّله يدوياً بعد كل commit (موثَّق كقيد بيئي دائم في `STATE.md`).
- الموضوعان "المخزون الراكد" و"أوامر الشراء" ما زالا قيد النقاش — **لم يُنشأ لهما أي أثر على المنصة**، بانتظار موافقة صريحة من GM.
